{
  "buildCommand": "pnpm install && pnpm --filter @workspace/alexa-gray run build",
  "outputDirectory": "artifacts/alexa-gray/dist/public",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "BASE_PATH": "/"
  }
}
